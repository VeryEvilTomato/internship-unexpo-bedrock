import React from 'react';

import {
    Button,
    Icon,
    Input,
} from 'react-native-elements';

import {
    View,
} from 'react-native';


export default function SplashScreen({navigation}) {
    return (
        <View>
            <Icon
                type="material"
                name="donut-small"
            />
        </View>
    );
}
